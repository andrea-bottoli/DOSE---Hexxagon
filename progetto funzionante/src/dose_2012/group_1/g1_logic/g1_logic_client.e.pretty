note
	description: "Summary description for {G1_LOGIC_CLIENT}."
	author: ""
	date: "$Date$"
	revision: "$Revision$"

class
	G1_LOGIC_CLIENT

create
	make

feature -- Attributes

	ip_local: INTEGER

feature -- Initialization

	make (local_ip: INTEGER)
			-- Initialize the logic_client.
		require
			valid_ip: local_ip > 0
		do
		ensure
			valid_ip: ip_local > 0 and ip_local = local_ip
		end

	mortgage
			-- Mortgage
		do
		end

	unmortage
			-- Unmortage
		do
		end

	trade
			-- Trade
		do
		end

	finish_turn
			-- Finish the turn of the current player
		do
		end

	build
			-- Build
		do
		end

	sell
			-- Sell
		do
		end

	lock_player
			-- Lock players
		do
		end

	unlock_player
			-- Unlock player
		do
		end

	getIp
			-- Get the local ip
		do
		end

	sendMessage
			--Send the message
		do
		end

end
