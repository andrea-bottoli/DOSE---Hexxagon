note
	description: "Summary description for {G21_NOTIFYIMPLEMENTOR}."
	author: ""
	date: "$Date$"
	revision: "$Revision$"

class
	G21_NOTIFYIMPLEMENTOR

	inherit
		G21_NOTIFYLISTENER

	redefine errorMessage,statusMessage,message  end


	feature
		errorMessage(msg : STRING)
		do

		end

		statusMessage(msg : STRING)
		do

		end

		message(msg : STRING)
		do

		end


end
