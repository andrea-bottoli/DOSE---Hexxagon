note
	description: "Summary description for {MATCH}."
	author: ""
	date: "$Date$"
	revision: "$Revision$"

class
	RI_MATCH
 create
 	make
  feature
  matchid: INTEGER
  model: RI_WORLDMODEL


  initiateANewMatch()
    do
      -- create a match
    end

  loadASavedMAtch()
    do
    	--load or save the actual or saved match
    end

  	make
  	 do
  	 	--initialize the instance
  	 end
end
